<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:zoom="clr-namespace:GraphSharp.Controls.Zoom;assembly=GraphSharp.Controls"
        xmlns:graph_component="clr-namespace:GraphComponent;assembly=GraphComponent"
        mc:Ignorable="d"
        Title="Graph Constructor" Height="{Binding SystemParameters.PrimaryScreenHeight}" Width="{Binding SystemParameters.PrimaryScreenWidth}">
    <Window.CommandBindings>
        <CommandBinding Command="Open" Executed="OpenFile_OnClick"/>
        <CommandBinding Command="Save" Executed="SaveFile_OnClick"/>
        <CommandBinding Command="Close" Executed="Close_OnClick"/>
    </Window.CommandBindings>
    <Window.Resources>
        <LinearGradientBrush x:Key="GrayGrayGradientBrush"      StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="DarkGray" Offset="0" />
            <GradientStop Color="White" Offset="0.5" />
            <GradientStop Color="DarkGray" Offset="1" />
        </LinearGradientBrush>
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource GrayGrayGradientBrush}" />
        </Style>
        <Style x:Key="RunButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Content" Value="Run"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Margin" Value="0,10,119,10"/>
            <Setter Property="RenderTransformOrigin" Value="0.127,0.68"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style x:Key="ToolBarButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="BorderBrush" Value="Gray"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Command="Open"/>
                <MenuItem Command="Save"/>
                <Separator />
                <MenuItem Command="Close"/>
            </MenuItem>
            <MenuItem Header="Options">
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="186"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" HorizontalAlignment="Right" Name="textEditor" Width="187"  Background="#FFF0F0F0" Margin="-1,0,0,0">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Label}">
                        <Setter Property="Margin" Value="0,5,0,0"/>
                    </Style>
                </StackPanel.Resources>
                <Separator />
                <TextBlock Background="{StaticResource GrayGrayGradientBrush}">
                    <Label Content="Workflow" FontSize="14" Background="Transparent" BorderBrush="Transparent" Width="73" Height="28" Margin="57,0"/>
                </TextBlock>
                <Separator />
                <Label Content="1. Construct by Prufer code" Height="28"></Label>
                <TextBlock Height="55" TextWrapping="WrapWithOverflow" Margin="18,0,35,0"><Run Text="Please type a code."/><LineBreak/><Run FontStyle="Italic" Text="As a separator use a semicolon or a space."/></TextBlock>
                <TextBox x:Name="PruferTextBox" Height="22" HorizontalAlignment="Right" Width="112" Margin="0,0,57,0"/>
                <Button x:Name="Construct" Style="{StaticResource RunButton}" Click="ConstructByPrufer_OnClick"/>
                <Label Content="2. Get Prufer code" Height="28"></Label>
                <Button x:Name="GetPrufer" Style="{StaticResource RunButton}" Click="GetPrufer_OnClick"/>
                <Label Content="3. Numerate Graph" Height="28"></Label>
                <Button x:Name="Numerate" Style="{StaticResource RunButton}" Click="Numerate_OnClick"/>
            </StackPanel>
            <TabControl Grid.Column="1">
                <TabItem Header="Graph">
                    <TabItem.Background>
                        <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                            <GradientStop Color="#FFF0F0F0" Offset="0"/>
                            <GradientStop Color="#19E5E5E5" Offset="1"/>
                        </LinearGradientBrush>
                    </TabItem.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="36"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Separator Grid.Row="0"/>
                        <Grid Grid.Row="0" Background="{StaticResource GrayGrayGradientBrush}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="53*" />
                                <ColumnDefinition Width="231*" />
                            </Grid.ColumnDefinitions>
                            <ToolBarTray Grid.Row="0" Grid.Column="0" Background="Transparent" HorizontalAlignment="Left">
                                <ToolBar Background="LightGray" Height="36">
                                    <Button Name="NewVertex" ToolTip="Create new vertex" Click="NewVertex_OnClick" Style="{StaticResource ToolBarButton}">
                                        <Image Source="/GUI;component/Resources/Icons/new_vertex.ico" Stretch="Fill"/>
                                    </Button>
                                    <Separator/>
                                    <Button Name="NewEdge" ToolTip="Create new edge" Click="NewEdge_OnClick" Style="{StaticResource ToolBarButton}">
                                        <Image Source="/GUI;component/Resources/Icons/new_edge.ico" Stretch="Fill"/>
                                    </Button>
                                    <Separator/>
                                    <Button Name="Refresh" ToolTip="Refresh layout" Click="Refresh_Click" Style="{StaticResource ToolBarButton}">
                                        <Image Source="/GUI;component/Resources/Icons/refresh.ico" Stretch="Fill"/>
                                    </Button>
                                </ToolBar>
                            </ToolBarTray>
                            <Label Grid.Column="1" x:Name="PruferResult" FontSize="15" HorizontalContentAlignment="Center" />
                        </Grid>
                        <Separator Grid.Row="1"/>
                        <zoom:ZoomControl Grid.Row="1" Zoom="0.2" MaxZoom="4">
                            <graph_component:GraphPane x:Name="GraphView"/>
                        </zoom:ZoomControl>
                    </Grid>
                </TabItem>
                <TabItem Header="Place holder for second tab">
                </TabItem>
                <TabItem Header="Place holder for third tab">
                </TabItem>
            </TabControl>
        </Grid>
    </DockPanel>
</Window>
